Ok, bài này bắt đầu giống workflow đi làm thật luôn 🔥
Không còn từng bước lẻ → mà là pipeline hoàn chỉnh.

# 📘 Chương 1 - Bài 13: Text Cleaning Pipeline (Chuẩn Production)

---

## 1. Text Cleaning Pipeline là gì?

**Text Cleaning Pipeline** là chuỗi các bước xử lý text theo thứ tự chuẩn:

👉 Input (raw text bẩn)  
→ xử lý nhiều bước  
→ Output (text sạch để đưa vào model)

---

## 2. Tại sao cần pipeline?

❗ Ngoài đời data rất bẩn:


"Code AI!!! mệt vl 😭😭 123 @@"


👉 Nếu không clean:
- model học sai
- noise cực nhiều

---

## 3. Pipeline chuẩn


Raw Text
→ Lowercase
→ Remove punctuation
→ Remove numbers
→ Remove extra spaces
→ Tokenization
→ Remove stopwords
→ (Optional) Stemming / Lemmatization
→ Clean Text


---

## 4. Ví dụ end-to-end

### Input:


"Tôi thích AI!!! vl 😆 123"


---

### Output:


"tôi thích ai vl"


---

## 5. Code Pipeline hoàn chỉnh

```python
import re

stop_words = ["tôi", "là", "và", "thì"]

def clean_text(text):
    # lowercase
    text = text.lower()
    
    # remove punctuation
    text = re.sub(r'[^\w\s]', '', text)
    
    # remove numbers
    text = re.sub(r'\d+', '', text)
    
    # remove extra spaces
    text = re.sub(r'\s+', ' ', text).strip()
    
    # tokenize
    tokens = text.split()
    
    # remove stopwords
    tokens = [w for w in tokens if w not in stop_words]
    
    return " ".join(tokens)


text = "Tôi   thích AI!!! vl 😆 123"
print(clean_text(text))
```
### 6. Pipeline thực tế (quan trọng)

👉 Không có pipeline chung cho mọi bài toán

🔹 Sentiment Analysis
giữ:
"không"
emoji
dấu "!!!"
🔹 Search Engine
clean mạnh
remove stopwords
🔹 Chatbot / LLM
giữ nguyên text càng nhiều càng tốt
### 7. Các bước nâng cao
🔹 Spell correction
"helo" → "hello"
🔹 Slang normalization
"vl" → "rất"
"ko" → "không"
🔹 Unicode normalization
🔹 Language detection
### 8. Best Practice (rất quan trọng)
✔ Luôn log pipeline
✔ Test từng bước
✔ Không clean quá mạnh
✔ Giữ version pipeline
### 9. Lỗi phổ biến
❌ Remove quá nhiều

→ mất nghĩa

❌ Không xử lý tiếng Việt đúng

→ tokenize sai

❌ Hardcode stopwords

→ không linh hoạt

### 10. Mini bài tập
Bài 1:

Clean câu:

"Code AI!!!   quá đỉnh 😎😎 999"
Bài 2:

Viết pipeline cho:

sentiment analysis
Bài 3:

Viết pipeline cho:

search engine
Bài 4 (xịn):

So sánh:

clean mạnh vs clean nhẹ

→ ảnh hưởng model như nào?

### 11. Tổng kết
Pipeline = backbone của NLP
Quyết định chất lượng model
Không có pipeline → model fail
🚀 Next:
